{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "lib/integrations/*",
    "scripts"
  ],
  "scripts": {
    "build:vercel": "npm -w @workspace/blood-strike-hub run build",
    "build": "npm run typecheck && npm run --workspaces --if-present build",
    "db:up": "docker compose up -d && npm -w @workspace/db run push",
    "db:push": "npm -w @workspace/db run push",
    "db:down": "docker compose down",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run --workspaces --if-present typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^5.9.2",
    "prettier": "^3.8.1",
    "@types/node": "^25.3.3"
  }
}
